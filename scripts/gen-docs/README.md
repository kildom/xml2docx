
# Documentation Generation Scripts

Generates documentation for the `DocTML` project.

Entry point is `gen-html.ts` which generates HTML documentation in the `../../dist/html` folder.

TODO: `gen-xsd.ts` will generate XSD schema that describes the `DocTML` document structure.
It can be used with many XML tools to validate `DocTML` documents.
